<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>gr8Art</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
   
<body>
    
    <%- include('../partials/_navbar.ejs') %> 
    <h1>Title: <%= art.title %></h1>

        <div class="flex-container">
            <%art.colorArray.forEach(color => { %>
                <div class="flex-item" style="background-color:<%=color %>"> </div>
              <%}); %>
        </div>
    <p>Description: <%= art.description %></p>
    <!-- placeholder for comment section -->
    <%= art.comments %>
    
    <!-- Kind of spaghetti code here, comparing an object (json?) to a string. can't use stringify here.) -->
    <% if( art.owner == owner){ %>
    
        <p>You made this!</p>

    <% } else { %>

        <p>you didn't make this</p>
    <% } %>


</body>